/*DISPLAYING STRINGG & TOTAL NUMBER OF CHARACTERS*/
#include<stdio.h>
#include<string.h>
main()
{
	int len;
	char arr[]=" good morning ibadee shaikh";
	len=strlen(arr);
	printf("string is : %s \n total characters are : %d",arr,len);
}
